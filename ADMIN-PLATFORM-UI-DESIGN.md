# uMaaS 管理平台 UI 设计方案

> 状态：执行方案待审阅 · 更新于 2026-09-06
> 范围：面向**平台管理者**的独立控制台，含多维数据分析与私有化模型部署两大核心。
> 不含：现有面向开发者的门户（`/`、`/models`、`/docs`、`/benchmarks`、`/rankings`、`/console`）。
> 落地位置：**`frontend/admin/`**（与 `frontend/web/` 平行的独立应用，见 §9）。

---

## 1. 为什么必须是独立的第三端

当前 `frontend/web/` 已有两端：

| 端 | 路由 | 使用者 | 心智 |
|---|---|---|---|
| 开发者门户 | `/`、`/models`、`/docs`、`/benchmarks`、`/rankings`、`/harness` | 匿名访客、潜在用户 | 选型、学习、被说服 |
| 用户自助控制台 | `/console/*` | 已注册开发者 | 管我自己的 key、用量、账单 |

管理者需要的是**第三种心智**：管别人的资源、管平台的供给、管物理机器。三者的差别不是权限开关能覆盖的：

- **数据尺度不同**：用户看"我的 1.28M 请求"，管理者看"全平台 8,400 用户 / 62 亿 tokens"，图表的聚合层级、时间粒度、下钻路径完全不同。
- **操作后果不同**：用户误点最多毁掉自己的 key；管理者误点会让**全平台模型下架**或**杀掉正在服务的 GPU 实例**。破坏性操作需要独立的确认层级、二次验证和审计留痕。
- **物理资源是全新维度**：GPU、显存、磁盘、下载带宽、进程生命周期——这些在现有两端里完全不存在，硬塞进 `/console` 会让侧边栏语义崩塌。

**结论**：独立路由前缀 `/admin/*`，独立的 `AdminShell`，独立视觉基调，与 `/console` 共享底层组件但不共享导航。

---

## 2. 信息架构

```
/admin
├── 总览            /admin                      平台健康度首屏
│
├── 数据分析
│   ├── 用量分析     /admin/analytics/usage      tokens / 请求 / 成本多维下钻
│   ├── 用户分析     /admin/analytics/users      规模、留存、分层
│   ├── 模型分析     /admin/analytics/models     模型热度、单位经济性
│   └── 工具调用     /admin/analytics/tools      function calling / MCP 维度
│
├── 供给管理
│   ├── 模型目录     /admin/catalog/models       上下架、能力标签、定价
│   ├── 供应商渠道   /admin/catalog/channels     端点、密钥、限流、健康
│   └── 路由策略     /admin/catalog/routing      权重、回退链、灰度
│
├── 客户管理
│   ├── 用户         /admin/customers/users      检索、分组、封禁、配额
│   ├── 工作空间     /admin/customers/workspaces 团队、席位、预算
│   └── 账务         /admin/customers/billing    充值、退款、发票、对账
│
├── 私有化部署  ★新增核心
│   ├── 集群总览     /admin/infra                节点、GPU 拓扑、水位
│   ├── 容量评估     /admin/infra/capacity       "这批卡能跑多大模型"
│   ├── 模型仓库     /admin/infra/registry       从开源社区拉取权重
│   ├── 部署实例     /admin/infra/deployments    发布、扩缩、灰度、回滚
│   ├── 性能压测     /admin/infra/benchmark      SLO 验证与容量定标
│   └── GPU 监控     /admin/infra/observability  健康、告警、故障归因
│
└── 系统
    ├── 平台设置     /admin/system/settings
    ├── 管理员与角色 /admin/system/rbac
    └── 审计日志     /admin/system/audit
```

**导航形态**：左侧两级折叠侧边栏（一级图标+文字，二级缩进），顶部保留全局搜索与环境切换器（生产/预发）。不用面包屑之外的三级导航——超过两级说明模块该拆了。

---

## 3. 设计语言

### 3.1 技术栈（两端统一，已定）

关键区分：**技术栈统一，UI 交互独立**。两者不矛盾——同一套库、同一批版本，但两个产品的导航、布局、组件、视觉各走各的，互不复用代码。

版本取 2026-09-06 实测的 npm 最新发布。**P-1 已执行完毕，web 现已全部运行在下表版本**，admin 直接采用同一张表：

| 层 | 选型 | 版本 | 状态 |
|---|---|---|---|
| 框架 | React | `19.2.8` | ✅ 原已最新 |
| 组件库 | Ant Design | `6.6.2` | ✅ 原已最新 |
| 图标 | @ant-design/icons | `6.3.4` | ✅ 原已最新 |
| 样式 | Tailwind CSS | `4.3.3` | ✅ 原已最新 |
| 图表 | Recharts | `3.10.1` | ✅ 原已最新 |
| 路由 | React Router | `8.3.1` | ⬆️ 从 `7.18.3` 升级 |
| 构建 | Vite | `8.2.2` | ⬆️ 从 `7.3.6` 升级 |
| React 插件 | @vitejs/plugin-react | `6.1.1` | ⬆️ 从 `5.2.0` 升级 |
| 语言 | TypeScript | `7.0.2` | ⬆️ 从 `5.9.3` 升级 |

前 5 项原本的 `^` 范围已自动覆盖最新版（实际安装即最新，只是 `package.json` 里的下限写的是旧值，现已一并对齐，便于两端版本比对）。后 4 项为显式跨大版本升级，执行记录与实际遇到的破坏性变更见 §10.P-1。

### 3.2 样式策略

技术栈统一不代表写法统一。这里两端有一个需要明确的实际差异：

web 虽然装了 Tailwind，但**实际几乎没用**——全量扫描仅 1 处 utility 类，布局全靠一个近 100 行超长行的手写 `index.css`，类名是 `.console-panel`、`.rank-entry` 这类语义类。这很可能不是主动选择，而是撞上了 Tailwind 与 antd 的层级冲突后的退让（见 §9.0.1）。

**admin 反过来，真正用 Tailwind utility-first**，语义类只在三种情况下才写：

1. 一个模式在 3 个以上页面重复出现，且 utility 串超过 ~10 个（抽成组件，而不是抽 CSS 类）；
2. Tailwind 表达不了的（复杂伪元素、`@keyframes`、打印样式、`forced-colors` 适配）；
3. 图表纹理填充等需要 SVG `<pattern>` 配合的。

**不要把 web 的 `console-*` 那套搬过来再用 Tailwind 包一层**——那会同时背上两套样式体系的成本，是最差的组合。

web 的手写 CSS 不在本方案范围内改动。若将来要统一写法，方向是 web 向 admin 看齐，而不是反过来。

admin 反过来：**Tailwind utility 为主，语义类只在三种情况下才写**——

1. 一个模式在 3 个以上页面重复出现，且 utility 串超过 ~10 个（抽成组件，而不是抽 CSS 类）；
2. Tailwind 表达不了的（复杂伪元素、`@keyframes`、打印样式、`forced-colors` 适配）；
3. 图表纹理填充等需要 SVG `<pattern>` 配合的。

**不要把 web 的 `console-*` 那套搬过来再用 Tailwind 包一层**——那会同时背上两套样式体系的成本，是最差的组合。

### 3.3 设计 token 的单一来源

Tailwind 4 是 CSS-first 配置，色板、间距、圆角在 `@theme` 里声明一次，同时供给 utility 类和 antd：

```css
/* admin/src/index.css */
@theme {
  /* 品牌与界面 */
  --color-brand:      #7c3cff;
  --color-ink:        #171719;
  --color-line:       #dedee5;
  --color-surface:    #ffffff;
  --color-plane:      #f7f7fa;

  /* §4 分类色板：固定 8 槽，顺序即语义 */
  --color-series-1: #2a78d6;  --color-series-5: #e87ba4;
  --color-series-2: #eb6834;  --color-series-6: #008300;
  --color-series-3: #1baf7a;  --color-series-7: #4a3aa7;
  --color-series-4: #eda100;  --color-series-8: #e34948;

  /* §4 状态色：保留，永不作系列色 */
  --color-status-good:     #0ca30c;
  --color-status-warning:  #fab219;
  --color-status-serious:  #ec835a;
  --color-status-critical: #d03b3b;

  /* §4 顺序色（热力图）单一蓝色相 */
  --color-seq-100: #cde2fb;  --color-seq-400: #3987e5;
  --color-seq-250: #86b6ef;  --color-seq-550: #1c5cab;
  --color-seq-700: #0d366b;
}
```

声明一次得到两样东西：`bg-series-1`、`text-status-critical` 这类 utility 类；以及喂给 antd `ConfigProvider` 的 token（读同一批 CSS 变量，不再手抄一份十六进制值）。**色值只有一处定义，改一次全站生效**——这是 admin 相对 web 最实质的改进，web 目前的色值散落在 CSS 与 `theme.ts` 两处。

### 3.4 视觉差异化

管理者需要一眼分辨"我在管理后台，不是在用户视图"。用**环境色带**而非换主题色：

- 顶栏下方 3px 色带：生产 `#7c3cff`，预发 `#eda100`，本地 `#898781`。
- 侧边栏顶部是**环境切换器**，明示当前操作的是哪套数据。
- 破坏性操作统一 `danger` + 输入资源名才可提交，与用户端的一次点击区分开。

---

## 4. 图表规范（已用工具实测，非经验值）

管理平台是图表密集型产品，配色和图表选型先定死，避免每个页面各画各的。

### 4.1 分类色板（用于"区分不同实体"）

固定 8 槽，**按顺序分配，绝不循环**。第 9 个系列一律折叠为「其他」或改用小倍数分面。

| 槽 | 色值 | 典型用途 |
|---|---|---|
| 1 | `#2a78d6` 蓝 | OpenAI / 主系列 |
| 2 | `#eb6834` 橙 | Google |
| 3 | `#1baf7a` 青 | Anthropic |
| 4 | `#eda100` 黄 | DeepSeek |
| 5 | `#e87ba4` 品红 | 阿里 / 智谱 |
| 6 | `#008300` 绿 | 其他闭源 |
| 7 | `#4a3aa7` violet | 自建私有模型 |
| 8 | `#e34948` 红 | 其他 |

**实测结果**（白色面板 `#ffffff` 为绘图面，色盲安全性用 OKLab ΔE 计算而非目测）：

```
[PASS] 明度带      8 色全部落在 L 0.43–0.77
[PASS] 彩度下限    8 色全部 >= 0.1
[PASS] 色盲分离度  最差相邻对 #eda100↔#1baf7a  ΔE 9.1 (protan)   目标 >=8
[PASS] 常视觉下限  最差相邻对 #e87ba4↔#eda100  ΔE 19.6            下限 >=15
[WARN] 与底色对比  低于 3:1 的三个槽：青 2.82、黄 2.17、品红 2.69
```

`WARN` 不可忽略，它触发**缓解规则**：用到槽 3/4/5 的图必须提供可见的直接标注或配套表格视图，不能只靠颜色识别。这条已写进下面每张图的规格。

散点图、气泡图、地图这类**任意两色都会同屏相邻**的形态，色板上限是**前 3 槽**（实测全配对 CVD ΔE 9.2 / 常视觉 24.0 通过）；超过 3 个系列必须分面。

### 4.2 其余三类色

- **顺序色**（表达"多少"，如热力图）：单一蓝色相由浅到深 `#cde2fb → #0d366b`。禁止彩虹色。
- **发散色**（表达"高于/低于基线"，如成本环比）：蓝 ↔ 红，中点必须是中性灰 `#f0efec`。
- **状态色**（保留，永不用作系列色）：正常 `#0ca30c`、警告 `#fab219`、严重 `#ec835a`、危急 `#d03b3b`。**必须图标+文字同时出现**，颜色不单独承载语义——GPU 健康状态尤其依赖这条。

单系列图表（如全平台请求趋势）直接用品牌紫 `#7c3cff`，白底对比度实测 5.32:1，安全。

### 4.3 图表选型硬规则

| 数据的任务 | 用 | 不用 |
|---|---|---|
| 单个当前值 + 趋势 | 统计磁贴（数值+环比+迷你走势） | 单柱柱状图 |
| 首屏最重要的那个数 | 主视觉大数字（≥48px） | — |
| 单个比率对上限（显存占用/配额） | 计量条 | 两片的饼图 |
| 随时间变化 | 折线；单系列用面积 | — |
| 区分多个实体 | 堆叠面积 / 多折线 + 分类色 | — |
| 网格状的量级对比（GPU×时间） | 热力图 + 顺序色 | — |
| 构成占比 | 堆叠条 | 饼图（>6 段） |
| 超过 7 个类别都有意义 | 表格，或表格+图 | 更多颜色 |

**一条绝对禁令：不做双 Y 轴图。** 两个不同量纲的指标（tokens 与成本、请求数与延迟）放一张图上，两个坐标轴的对齐方式是任意的，会凭空制造出数据里不存在的相关性。做法是：拆成两张上下对齐的图，或者把两条线都归一化到 t0=100 后画在同一轴上。这是本方案里唯一不接受妥协的图表规则。

同量纲的可以共轴：GPU 利用率% 与显存占用% 都是 0–100，画一张图两条线是正确的。

### 4.4 交互基线

所有折线/面积图默认带十字准星+浮层提示；柱/点/热力单元默认逐格悬浮提示。筛选控件统一放在图表区上方一行。≥2 个系列必须有图例，≤4 个系列同时做直接标注。每张图提供「查看数据表」入口（同时满足对比度缓解规则和无障碍要求）。

---

## 5. 模块设计

### 5.1 总览 `/admin`

回答一个问题：**平台现在健不健康，要不要我立刻处理什么。**

```
┌────────────────────────────────────────────────────────────────────────────┐
│ 生产环境 ▾          [搜索用户/模型/实例  ⌘K]         🔔 3    管理员 ▾      │
├──────────── 3px 环境色带 (#7c3cff) ────────────────────────────────────────┤
│        │                                                                    │
│ 总览   │  平台总览                                    今日 · UTC+8  [刷新]  │
│        │  ──────────────────────────────────────────────────────────────   │
│ 数据   │                                                                    │
│  分析▾ │   ┌─ 需要处理 ─────────────────────────────────────────────────┐  │
│ 供给   │   │ ⚠ 危急  node-04 GPU3 显存 ECC 双位错误，实例已自动驱逐  →  │  │
│  管理▾ │   │ ⚠ 警告  Anthropic 渠道 5 分钟错误率 12.4%，已降权       →  │  │
│ 客户   │   │ ⚠ 警告  用户 acme-ai 余额 -$3.20，仍在调用             →  │  │
│  管理▾ │   └────────────────────────────────────────────────────────────┘  │
│ 私有化 │                                                                    │
│  部署▾ │   ┌──────────┬──────────┬──────────┬──────────┬──────────┐        │
│ 系统▾  │   │ 今日请求 │ 今日Tokens│ 今日成本 │ 活跃用户 │ GPU水位  │        │
│        │   │  4.82M   │  1.94B   │ $2,418   │  1,203   │  73%     │        │
│        │   │ ↑12.4%   │ ↑18.1%   │ ↑9.7%    │ ↑3.2%    │ 24/33卡  │        │
│        │   └──────────┴──────────┴──────────┴──────────┴──────────┘        │
│        │                                                                    │
│        │   ┌─ 请求量 24h ───────────────┐ ┌─ 供给健康 ────────────────┐   │
│        │   │                            │ │ 渠道         状态   P95   │   │
│        │   │      ╱╲    ╱╲╱╲            │ │ ● OpenAI     正常   412ms │   │
│        │   │  ╱╲╱   ╲╱      ╲           │ │ ● Google     正常   388ms │   │
│        │   │ ╱               ╲          │ │ ▲ Anthropic  降级  1.2s   │   │
│        │   │  单系列·品牌紫·十字准星    │ │ ● 自建 vLLM  正常   96ms  │   │
│        │   └────────────────────────────┘ └───────────────────────────┘   │
│        │                                                                    │
│        │   ┌─ 厂商 Token 份额 7d ───────┐ ┌─ 集群水位 ────────────────┐   │
│        │   │ 堆叠面积·分类色 1-5+其他   │ │ 显存 ████████░░ 73%       │   │
│        │   │ 图例+前4直接标注           │ │ 算力 ██████░░░░ 61%       │   │
│        │   │ [查看数据表]               │ │ 磁盘 ███░░░░░░░ 34%       │   │
│        │   └────────────────────────────┘ └───────────────────────────┘   │
└────────┴────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **告警区置顶且可为空**。没有告警时整块消失，不留占位——空状态不是内容。
- KPI 行用统计磁贴，不是 5 个小柱状图。
- 请求量单系列，用品牌紫。份额图多系列，用分类色板并折叠长尾为「其他」。
- 「GPU 水位 73%」是计量条不是饼图。

### 5.2 用量分析 `/admin/analytics/usage`

多维下钻的主战场。核心是**一套筛选器 + 一个可切换的度量 + 一个可切换的分组维度**，而不是把 20 张图堆在一页。

```
┌─ 用量分析 ─────────────────────────────────────────────────────────────────┐
│                                                                            │
│ 时间 [近30天 ▾] 粒度 [天 ▾] 分组 [按厂商 ▾] 模型[全部▾] 用户组[全部▾] [导出]│
│ ────────────────────────────────────────────────────────────────────────── │
│                                                                            │
│  度量  ( ●Tokens  ○请求数  ○成本  ○工具调用 )        单位 (●绝对 ○占比)   │
│                                                                            │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │  1.94B ← 主视觉大数字，当期总量                        ↑18.1% 环比    │ │
│  │                                                                      │ │
│  │   ▁▂▃▅▆█  堆叠面积图                                                │ │
│  │   分类色按厂商固定分配（OpenAI 恒为蓝，筛掉别的也不变色）             │ │
│  │   ≤4 系列直接标注在末端，>4 走图例                                   │ │
│  │                                                       [查看数据表]   │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 输入/输出构成 ──────────┐  ┌─ 时段热力 ─────────────────────────────┐ │
│  │ 堆叠条：Prompt / Output / │  │      0 2 4 6 8 10 ... 22               │ │
│  │ Cache-read / Cache-write  │  │ 周一 ░░▒▒▓▓██▓▓▒▒░░  顺序色单蓝色相   │ │
│  │ 4 段，直接标注百分比      │  │ 周二 ░░▒▓██████▓▒░░  含刻度图例        │ │
│  └───────────────────────────┘  └────────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 明细（可下钻） ─────────────────────────────────────────────────────┐ │
│  │ 分组         Tokens    请求数    成本     均延迟  错误率   占比       │ │
│  │ OpenAI       842.1M    1.94M     $1,204   412ms   0.31%   ███░ 43.4% │ │
│  │  └ gpt-5.6   612.4M    1.41M     $  902   398ms   0.28%   ██░  31.6% │ │
│  │ Google       501.2M    1.22M     $  418   388ms   0.19%   ██░  25.8% │ │
│  │ ...                                                     [导出 CSV]   │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **度量切换而非并列**：Tokens 和成本是不同量纲，绝不放一张图。切换 radio 复用同一张图，避免双轴陷阱。
- **「占比」单位切换**：绝对值看规模，百分比看结构，两种视角同一份数据。
- 分组维度可切：厂商 / 模型 / 用户组 / 应用 / 区域 / 密钥。
- 表格与图共存，既是下钻入口也是对比度缓解手段。

### 5.3 用户分析 `/admin/analytics/users`

```
┌─ 用户分析 ─────────────────────────────────────────────────────────────────┐
│  ┌────────┬────────┬────────┬────────┬────────┐                           │
│  │总注册  │本月新增│日活    │付费用户│ARPU    │                           │
│  │ 8,412  │ +642   │ 1,203  │  318   │ $42.6  │                           │
│  └────────┴────────┴────────┴────────┴────────┘                           │
│                                                                            │
│  ┌─ 增长（新增 vs 流失） ───────┐ ┌─ 留存队列热力 ──────────────────────┐ │
│  │ 发散条形：新增向上(蓝)       │ │        W0  W1  W2  W3  W4           │ │
│  │ 流失向下(红)，中性灰基线     │ │ 07-01 100  62  48  41  38  顺序蓝    │ │
│  │ 净增长折线叠加               │ │ 07-08 100  58  45  39  --           │ │
│  └──────────────────────────────┘ └─────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 用户分层（用量分位） ───────┐ ┌─ 转化漏斗 ──────────────────────────┐ │
│  │ 横向堆叠条                   │ │ 注册 8,412 ████████████             │ │
│  │ 免费/自助/团队/企业          │ │ 建Key 5,108 ███████                 │ │
│  │ 每段直接标数量               │ │ 首调用3,942 ██████   序数色阶      │ │
│  └──────────────────────────────┘ │ 付费   318 █                        │ │
│                                    └─────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

### 5.4 工具调用分析 `/admin/analytics/tools`

这一维度现有产品普遍缺失，但对判断"用户在拿模型做什么"最有价值。

```
┌─ 工具调用分析 ─────────────────────────────────────────────────────────────┐
│  ┌────────────┬────────────┬────────────┬────────────┐                    │
│  │ 工具调用数 │ 平均每会话 │ 调用成功率 │ 含工具请求 │                    │
│  │   18.4M    │    3.7     │   94.2%    │   38.1%    │                    │
│  └────────────┴────────────┴────────────┴────────────┘                    │
│                                                                            │
│  ┌─ 调用量 Top 工具 ────────────────────────────────────────────────────┐ │
│  │ read_file       ████████████████████  4.82M   成功 98.1%  均 42ms    │ │
│  │ bash            ███████████████       3.61M   成功 91.4%  均 1.2s    │ │
│  │ web_search      ██████████            2.44M   成功 96.7%  均 890ms   │ │
│  │ edit_file       ████████              1.98M   成功 93.2%  均 61ms    │ │
│  │  ← 单一色相（槽1蓝），不做"越大越深"的值阶映射                       │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 按 Agent 框架 ──────────────┐ ┌─ 工具调用链长度分布 ────────────────┐ │
│  │ Claude Code  ████████ 38.2%  │ │ 1步 ████████████                    │ │
│  │ uCodex       ██████   27.4%  │ │ 2-3 ████████                        │ │
│  │ OpenCode     ████     18.1%  │ │ 4-7 █████     序数色阶（有序类别）  │ │
│  │ Pi           ███      11.9%  │ │ 8+  ██                              │ │
│  │ 其他         █         4.4%  │ └─────────────────────────────────────┘ │
│  └──────────────────────────────┘                                          │
└────────────────────────────────────────────────────────────────────────────┘
```

Agent 框架维度依赖请求头或 `user-agent` 识别，需要在网关侧埋点（见 §8 指标字典）。

### 5.5 模型目录管理 `/admin/catalog/models`

回答"如何及时更新模型"。核心是**同步 + 差异审阅 + 灰度上架**三步，而不是让管理者手工填表单。

```
┌─ 模型目录 ─────────────────────────────────────────────────────────────────┐
│  431 个模型 · 上架 398 · 灰度 12 · 下架 21    [从上游同步] [+ 手工添加]    │
│                                                                            │
│  [搜索] [厂商▾] [状态▾] [能力▾] [类型: 云端/自建▾]                        │
│  ────────────────────────────────────────────────────────────────────────  │
│  □ 模型              厂商      状态    上下文  输入价  输出价  近7d调用    │
│  □ ◆ gpt-5.6-sol    OpenAI    ●上架   400K   $1.25  $10.00   1.41M       │
│  □ ◆ claude-opus-5  Anthropic ●上架   1M     $5.00  $25.00   0.98M       │
│  □ ◆ qwen3-max      自建      ◐灰度10% 256K  自建    自建     12.4K       │
│  □ ◆ gemini-3.8-pro Google    ○下架   2M     $2.50  $15.00   0            │
│                                                       [批量操作 ▾]        │
└────────────────────────────────────────────────────────────────────────────┘
```

**「从上游同步」是这个模块的关键交互**，它不能是一个直接写库的按钮：

```
┌─ 上游同步 · 差异审阅 ──────────────────────────────────────────── [×] ────┐
│  来源：OpenAI /v1/models · 抓取于 2 分钟前                                 │
│                                                                            │
│  ┌─ 新增 3 ──────────────────────────────────────────────────────────┐    │
│  │ ☑ gpt-5.7-preview    上下文 400K   需补充：定价、能力标签         │    │
│  │ ☑ o5-mini            上下文 200K   需补充：定价                   │    │
│  └───────────────────────────────────────────────────────────────────┘    │
│  ┌─ 变更 2 ──────────────────────────────────────────────────────────┐    │
│  │ ☑ gpt-5.6-sol   输入价  $1.25 → $1.10   ↓12%                     │    │
│  │ ☐ claude-opus-5 上下文  1M → 2M                                   │    │
│  └───────────────────────────────────────────────────────────────────┘    │
│  ┌─ 上游已消失 1 ────────────────────────────────────────────────────┐    │
│  │ ☐ gpt-4.1-legacy  近7d 仍有 2.4K 调用 ⚠ 建议先公告再下架          │    │
│  └───────────────────────────────────────────────────────────────────┘    │
│                                                                            │
│  应用方式  ( ●直接上架  ○先进灰度10%  ○仅创建草稿 )                       │
│                                          [取消]  [应用选中的 4 项变更]    │
└────────────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **差异先审后用**。上游改价、删模型都可能直接影响线上计费，不能自动生效。
- **消失的模型带上"近7天仍有调用"**，这是防止误下架的关键信息。
- 支持**定时同步 + 仅通知不自动应用**，管理者在告警区看到"有 3 项上游变更待审"。

### 5.6 供应商渠道 `/admin/catalog/channels`

```
┌─ 渠道 ─────────────────────────────────────────────────────────────────────┐
│  渠道         类型      优先级 权重  状态      P95    错误率  今日成本      │
│  OpenAI-主     官方      1     60   ●正常     412ms  0.31%   $1,204       │
│  OpenAI-备     Azure     2     40   ●正常     468ms  0.42%   $  318       │
│  Anthropic-主  官方      1     100  ▲降级     1.2s   12.4%   $  902       │
│    └ 自动降权中：错误率超阈值 5%，已切至备用渠道                           │
│  自建-vLLM     私有      1     100  ●正常      96ms  0.02%   —            │
│                                                    [+ 新建渠道] [测试全部] │
└────────────────────────────────────────────────────────────────────────────┘
```

渠道详情页含：端点与密钥（脱敏+轮换）、支持模型映射、限流配额、超时与重试、健康检查策略、**连通性测试**（发一条真实请求，返回延迟与原始响应）。

---

## 6. 私有化部署模块（新增核心）

这是整个方案里现有开源参考项目都没有覆盖的部分（`opensource/new-api` 的 `deployment.go` 只对接 io.net 第三方 GPU 云，不管理自有集群）。五个子页面构成一条完整链路：

```
容量评估 → 模型仓库(下载) → 部署实例 → 性能压测 → GPU监控
   ↑                                                    │
   └──────────────── 实测数据回流修正估算 ───────────────┘
```

### 6.1 集群总览 `/admin/infra`

```
┌─ 集群总览 ─────────────────────────────────────────────────────────────────┐
│  ┌────────┬────────┬──────────┬──────────┬────────┐                       │
│  │ 节点   │ GPU    │ 已分配   │ 显存水位 │ 运行实例│                       │
│  │  5     │ 33/40  │  24 卡   │   73%    │   8    │                       │
│  │        │ 7卡故障│          │          │        │                       │
│  └────────┴────────┴──────────┴──────────┴────────┘                       │
│                                                                            │
│  ┌─ 节点与 GPU 拓扑 ────────────────────────────────────────────────────┐ │
│  │ node-01  8×H100-80G  NVLink全互联                                    │ │
│  │  ┌────┬────┬────┬────┬────┬────┬────┬────┐                          │ │
│  │  │ 0  │ 1  │ 2  │ 3  │ 4  │ 5  │ 6  │ 7  │  每格：利用率热力+状态点  │ │
│  │  │███ │███ │███ │███ │▒▒▒ │▒▒▒ │░░░ │░░░ │  顺序蓝色相=利用率        │ │
│  │  │ ●  │ ●  │ ●  │ ●  │ ●  │ ●  │ ●  │ ●  │  状态点=健康(图标+悬浮名) │ │
│  │  └────┴────┴────┴────┴────┴────┴────┴────┘                          │ │
│  │  └─ qwen3-72b (TP=4, GPU0-3)  └─ 空闲                                │ │
│  │                                                                       │ │
│  │ node-04  8×A100-40G  PCIe                                            │ │
│  │  ┌────┬────┬────┬────┬────┬────┬────┬────┐                          │ │
│  │  │███ │███ │ ✕  │░░░ │░░░ │░░░ │░░░ │░░░ │  ✕ = 故障，红色+图标+文字 │ │
│  │  └────┴────┴────┴────┴────┴────┴────┴────┘                          │ │
│  │  ⚠ GPU2 ECC 双位错误，已隔离                                [详情 →] │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

拓扑图的颜色分工必须清晰：**格子填充=利用率（顺序色，连续量）**，**角标=健康状态（状态色，离散态，必带图标）**。两个通道不能混用同一种颜色语义。

### 6.2 容量评估 `/admin/infra/capacity`

回答"这批 GPU 最大能跑多大模型"。这是一个**双向计算器**：给模型算需要多少卡，或给卡算能跑多大模型。

```
┌─ 容量评估 ─────────────────────────────────────────────────────────────────┐
│  模式  ( ●模型 → 需要多少卡    ○现有卡 → 能跑多大模型 )                    │
│  ─────────────────────────────────────────────────────────────────────────│
│  ┌─ 输入 ──────────────────────┐  ┌─ 估算结果 ─────────────────────────┐ │
│  │ 模型   [Qwen3-72B      ▾]   │  │                                    │ │
│  │ 参数量  72.7 B  (自动读取)  │  │   需要 4 × H100-80G                │ │
│  │ 层数    80                   │  │   张量并行 TP=4                    │ │
│  │ KV头数  8  (GQA)             │  │                                    │ │
│  │ 头维度  128                  │  │  显存拆解（单卡）                  │ │
│  │                              │  │  权重      36.4 GB ███████████     │ │
│  │ 精度   [FP16 ▾] BF16/FP8/INT4│  │  KV Cache  22.5 GB ███████         │ │
│  │ 最大上下文 [32768    ]       │  │  激活/临时  3.6 GB ██              │ │
│  │ 目标并发   [64       ]       │  │  框架开销   2.0 GB █               │ │
│  │ 显存余量   [10%      ]       │  │  ─────────────────────────         │ │
│  │ 可用节点  [node-01   ▾]      │  │  合计      64.5 GB / 80 GB  81%    │ │
│  │                              │  │  ████████████████░░░░  计量条      │ │
│  │        [重新计算]            │  │                                    │ │
│  └──────────────────────────────┘  │  ✓ 可部署   余量 15.5GB            │ │
│                                     └────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 并发 / 上下文 可行域 ───────────────────────────────────────────────┐ │
│  │ 并发                                                                  │ │
│  │  128 │ ░░░░░░░░████████    热力图：顺序蓝=显存占用率                │ │
│  │   64 │ ░░░░▒▒▒▒▒▒▒▒████    深色=超出容量（叠加斜线纹理，不只靠色）  │ │
│  │   32 │ ░░░░░░▒▒▒▒▒▒▒▒▓▓    ★ = 当前配置点                          │ │
│  │   16 │ ░░░░░░░░▒▒▒▒▒▒▒▒                                             │ │
│  │      └─────────────────────                                          │ │
│  │        4K  8K  16K 32K 64K  上下文长度            [查看数据表]       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 精度方案对比 ───────────────────────────────────────────────────────┐ │
│  │ 精度   显存/卡  最少卡数  预估吞吐   质量损失   建议                 │ │
│  │ FP16   64.5GB      4      ~2,100t/s   基准     ● 推荐（质量优先）    │ │
│  │ FP8    41.2GB      2      ~3,400t/s   ~0.5%    ● 推荐（性价比）      │ │
│  │ INT4   24.8GB      1      ~2,800t/s   ~2-4%    ○ 仅限成本敏感场景    │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                     [用此配置去部署 →]     │
└────────────────────────────────────────────────────────────────────────────┘
```

**估算模型（需在 UI 上以「计算依据」折叠面板明示，不做黑盒）：**

```
总显存 ≈ 权重 + KV Cache + 激活 + 框架开销

权重     = 参数量 × 每参数字节数         (FP16=2, FP8=1, INT4≈0.5)
KV Cache = 2 × 层数 × KV头数 × 头维度 × 上下文长度 × 并发数 × 精度字节数
           （GQA/MQA 用 KV 头数而非注意力头数，这是最容易估错的一项）
激活     ≈ 权重 × 5~10%
框架开销 ≈ 1~2 GB/卡（CUDA context + NCCL 通信缓冲）

最少卡数 = ceil(总显存 / (单卡显存 × (1 - 余量比例)))
约束：TP 数需整除 KV 头数，且通常取 1/2/4/8
```

**必须标注估算的不确定性**：结果卡片上写明"估算值，实际占用受推理框架、PagedAttention 分页策略、CUDA Graph 影响，建议部署后以压测实测校准"。给出数字而不给出误差范围是不负责任的。压测完成后，实测值应回写到这个页面作为该模型的校准基准。

### 6.3 模型仓库 `/admin/infra/registry`

从开源社区拉取权重。核心难点是**大文件下载的可观测性**——72B 模型 FP16 约 145GB，下载要几十分钟到几小时，全程必须可见、可续、可取消。

```
┌─ 模型仓库 ─────────────────────────────────────────────────────────────────┐
│  本地 12 个模型 · 占用 2.4 TB / 8 TB              [+ 从社区拉取]           │
│  ─────────────────────────────────────────────────────────────────────────│
│  ┌─ 下载中 2 ────────────────────────────────────────────────────────────┐│
│  │ Qwen/Qwen3-72B-Instruct                                     [暂停][×] ││
│  │ ████████████████████░░░░░░░░  68%   98.4/145 GB              ↓ 82MB/s ││
│  │ 分片 21/32 · 剩余约 9 分钟 · 源 ModelScope 镜像                       ││
│  │                                                                        ││
│  │ deepseek-ai/DeepSeek-V4                                     [暂停][×] ││
│  │ ███░░░░░░░░░░░░░░░░░░░░░░░░░  11%   34.2/312 GB              ↓ 45MB/s ││
│  └────────────────────────────────────────────────────────────────────────┘│
│                                                                            │
│  本地模型                                                                  │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ 模型                    参数   精度  大小    校验   来源      已部署  │ │
│  │ Qwen3-32B-Instruct      32B   FP16  64.2GB  ✓通过  HF        ●2实例  │ │
│  │ Llama-4-70B             70B   FP16  141GB   ✓通过  HF        ○      │ │
│  │ DeepSeek-V4-Lite        16B   FP8   16.8GB  ✓通过  ModelScope ●1实例 │ │
│  │ glm-5-9b                9B    FP16  18.1GB  ⚠未校验 手工上传  ○      │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

拉取对话框：

```
┌─ 从开源社区拉取 ──────────────────────────────────────────── [×] ─────────┐
│  来源  ( ●HuggingFace  ○ModelScope  ○自定义 Git 地址 )                    │
│                                                                            │
│  仓库  [Qwen/Qwen3-72B-Instruct                        ] [解析]           │
│                                                                            │
│  ┌─ 已解析 ──────────────────────────────────────────────────────────┐   │
│  │ Qwen3-72B-Instruct · 72.7B 参数 · Apache-2.0                      │   │
│  │ 分支 [main ▾]   修订 [最新 ▾]                                     │   │
│  │ 权重格式 safetensors · 32 分片 · 合计 145.2 GB                    │   │
│  │ config.json 已读取：80 层 / 64 头 / 8 KV头 / 128 头维度           │   │
│  │                                                                    │   │
│  │ 磁盘检查  可用 5.6 TB，需要 145.2 GB           ✓ 空间充足         │   │
│  │ 许可证    Apache-2.0                            ✓ 无需接受协议     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                            │
│  加速  ☑ 使用国内镜像端点     ☑ 并发分片下载 [8 ▾]    ☑ 下载后校验 SHA256 │
│  凭据  访问令牌 [                    ] （门控仓库需要）                    │
│  存储  [/data/models ▾]  剩余 5.6TB                                       │
│                                                    [取消]  [开始拉取]      │
└────────────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **先解析后下载**。解析 `config.json` 拿到层数/头数，直接喂给容量评估器，形成闭环——用户不必手抄模型结构参数。
- **门控模型**（如 Llama 系列需接受协议）要在解析阶段就检测出来并提示，而不是下到 90% 才 403。
- 断点续传、分片并发、镜像源、SHA256 校验是刚需，不是加分项。
- 磁盘配额预检查，避免下到一半撑爆盘。

### 6.4 部署实例 `/admin/infra/deployments`

```
┌─ 部署实例 ─────────────────────────────────────────────────────────────────┐
│  8 个实例运行中                                              [+ 新建部署]  │
│  ┌──────────────────────────────────────────────────────────────────────┐ │
│  │ ● qwen3-72b-prod        vLLM 0.9   node-01 GPU0-3 (TP=4)             │ │
│  │   健康 ✓  QPS 42  P95 890ms  显存 64.5/80GB  KV命中 71%   [详情][…] │ │
│  │   ████████████████░░░░  副本 2/2                                     │ │
│  │                                                                       │ │
│  │ ◐ deepseek-v4-canary    SGLang    node-02 GPU0-7 (TP=8)              │ │
│  │   灰度 10%  QPS 4  P95 1.1s  显存 288/320GB              [详情][…]  │ │
│  │                                                                       │ │
│  │ ✕ glm-5-9b-test         vLLM 0.9   node-04 GPU2      启动失败        │ │
│  │   ⚠ CUDA OOM：请求 24GB，可用 18GB          [查看日志] [调整配置]    │ │
│  └──────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

新建部署为分步向导（模型 → 资源 → 运行参数 → 服务接入 → 预检），最后一步是**预检清单**而非直接启动：

```
┌─ 预检 ────────────────────────────────────────────────────────────────┐
│  ✓ 权重完整性       32/32 分片，SHA256 校验通过                       │
│  ✓ GPU 可用         node-01 GPU0-3 空闲，NVLink 全互联                │
│  ✓ 显存充足         预计 64.5GB / 单卡 80GB（81%）                    │
│  ✓ 端口可用         8081 未占用                                        │
│  ⚠ TP 约束          TP=4 整除 KV头数 8 ✓；注意力头 64 ✓               │
│  ⚠ 首次启动         权重加载预计 3–5 分钟，期间健康检查会失败          │
│                                                  [返回修改] [确认部署] │
└───────────────────────────────────────────────────────────────────────┘
```

部署过程展示**实时日志流**（终端样式，可搜索、可下载），加载阶段用不确定进度而非假进度条。

### 6.5 性能压测 `/admin/infra/benchmark`

```
┌─ 性能压测 ─────────────────────────────────────────────────────────────────┐
│  目标 [qwen3-72b-prod ▾]  场景 [对话 ▾]  [+ 新建压测]                     │
│                                                                            │
│  ┌─ 压测配置 ────────────────────────────────────────────────────────────┐│
│  │ 并发爬坡  [1,2,4,8,16,32,64,128]     输入长度 [1024]  输出长度 [512]  ││
│  │ 每档时长  [60s]   预热 [30s]         数据集 [ShareGPT 采样 ▾]         ││
│  │ SLO 目标  TTFT P95 < [500]ms   TPOT P95 < [50]ms                      ││
│  └───────────────────────────────────────────────────────────────────────┘│
│                                                                            │
│  ┌─ 最近一次结果 · 2026-09-05 14:22 ────────────────────────────────────┐ │
│  │                                                                       │ │
│  │   最大可用并发 48        ← 主视觉大数字（满足 SLO 的最高并发）       │ │
│  │   有效吞吐 2,140 tok/s   饱和吞吐 2,890 tok/s @ 并发128（已违 SLO）  │ │
│  │                                                                       │ │
│  │  ┌─ 吞吐 vs 并发 ─────────┐  ┌─ 延迟分位 vs 并发 ─────────────────┐ │ │
│  │  │      ╱‾‾‾‾‾╲            │  │        TTFT P99 ╱                  │ │ │
│  │  │    ╱        ‾╲          │  │    P95 ╱‾‾╱                        │ │ │
│  │  │  ╱             ╲        │  │  P50 ─╱                            │ │ │
│  │  │ 单系列·品牌紫          │  │  ┈┈┈┈┈┈┈┈ SLO线 500ms（灰虚线）   │ │ │
│  │  │ ★48 标注SLO拐点        │  │  同量纲(ms)共轴，3条线+图例        │ │ │
│  │  └────────────────────────┘  └────────────────────────────────────┘ │ │
│  │  （吞吐 tok/s 与延迟 ms 量纲不同，拆成两张上下对齐的图，不做双轴）  │ │
│  │                                                                       │ │
│  │  ┌─ 分档明细 ──────────────────────────────────────────────────────┐│ │
│  │  │ 并发  吞吐t/s  TTFT_P95  TPOT_P95  E2E_P95  错误率  SLO         ││ │
│  │  │   16   1,180     212ms     28ms     1.2s    0.0%   ✓通过        ││ │
│  │  │   32   1,840     341ms     39ms     2.1s    0.0%   ✓通过        ││ │
│  │  │   48   2,140     468ms     47ms     3.0s    0.0%   ✓通过        ││ │
│  │  │   64   2,420     712ms     63ms     4.4s    0.1%   ✕TTFT超标    ││ │
│  │  │  128   2,890    1,840ms   118ms    12.1s    1.8%   ✕严重超标    ││ │
│  │  └─────────────────────────────────────────────────────────────────┘│ │
│  │                            [对比历史] [导出报告] [回写容量评估基准]  │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **主结论是"最大可用并发"这一个数**，不是一堆曲线。曲线是佐证。
- **SLO 拐点**（吞吐仍在涨但延迟已违约的那一点）必须显式标注，这是容量规划真正要的数字。
- 吞吐（tok/s）和延迟（ms）**分成两张图**——这正是双轴陷阱最容易发生的地方。
- 结果可**回写容量评估器**作为该模型的实测基准，闭合 §6.2 的估算误差。

### 6.6 GPU 监控 `/admin/infra/observability`

```
┌─ GPU 监控 ─────────────────────────────────────────────────────────────────┐
│  节点 [全部 ▾]  时间 [近6小时 ▾]  刷新 [30s ▾]                            │
│                                                                            │
│  ┌─ 健康概览 ────────────────────────────────────────────────────────────┐│
│  │  ● 正常 31    ▲ 警告 1    ✕ 危急 1        ← 状态色+图标+文字三重编码 ││
│  │                                                                        ││
│  │  ✕ 危急  node-04 GPU2  ECC 双位错误 ×3      实例已驱逐    [处理 →]   ││
│  │  ▲ 警告  node-02 GPU5  温度 87°C 持续 12 分钟  已降频      [详情 →]   ││
│  └────────────────────────────────────────────────────────────────────────┘│
│                                                                            │
│  ┌─ 利用率与显存（同为 %，共轴合法）────────────────────────────────────┐│
│  │ 100%│                                                                 ││
│  │     │  ╱‾‾╲__╱‾‾‾╲    SM 利用率（蓝 槽1）                            ││
│  │  50%│ ╱      ‾‾    ╲   显存占用（橙 槽2）                             ││
│  │     │╱                 图例 + 末端直接标注                            ││
│  │   0%└──────────────────────────────────────                          ││
│  └────────────────────────────────────────────────────────────────────────┘│
│                                                                            │
│  ┌─ 温度 (°C) ──────────────┐  ┌─ 功耗 (W) ────────────────────────────┐ │
│  │ 独立成图（量纲不同）      │  │ 独立成图                              │ │
│  │ 阈值线 85°C 灰虚线        │  │ 阈值线 700W                           │ │
│  └───────────────────────────┘  └───────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 全集群 GPU × 时间 利用率热力 ───────────────────────────────────────┐ │
│  │           00:00  01:00  02:00  03:00  04:00  05:00                    │ │
│  │ n01-gpu0  ░░ ▒▒ ▓▓ ██ ██ ▓▓    顺序单蓝色相 + 刻度图例               │ │
│  │ n01-gpu1  ░░ ▒▒ ▓▓ ██ ██ ▓▓    悬浮显示精确值                        │ │
│  │ n04-gpu2  ✕✕ ✕✕ ✕✕ ✕✕ ✕✕ ✕✕    故障：斜线纹理+危急色，不只靠颜色    │ │
│  │                                                    [查看数据表]       │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│  ┌─ 硬件明细 ───────────────────────────────────────────────────────────┐ │
│  │ GPU      型号     显存        利用率 温度  功耗  ECC  XID  状态      │ │
│  │ n01-0    H100-80  64.5/80GB    92%   72°C  640W   0    -   ●正常     │ │
│  │ n02-5    H100-80  71.2/80GB    88%   87°C  698W   0    -   ▲高温     │ │
│  │ n04-2    A100-40   0.0/40GB     0%    41°C   62W   3   48   ✕故障    │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────┘
```

设计要点：
- **利用率与显存占用同为百分比，共用一轴合法**；温度、功耗量纲不同，各自独立成图。
- **故障格子叠加斜线纹理**，不能只靠红色——这是色觉障碍用户和黑白打印的唯一保障。
- 指标建议来源 DCGM Exporter：SM 利用率、显存、温度、功耗、SM 时钟、ECC 单/双位错误、XID 错误码、NVLink/PCIe 带宽、降频原因。
- **XID 错误码要给出人类可读的解释**（如 XID 48 = 双位 ECC 错误），而不是让管理者去查表。

---

## 7. 关键流程

**流程 A：上线一个私有模型（端到端）**

```
容量评估器输入 Qwen3-72B → 得出 TP=4 / 4×H100 → [用此配置去部署]
   ↓
模型仓库解析 HF 仓库（自动回填层数/头数）→ 拉取 145GB → SHA256 校验
   ↓
新建部署向导（预填容量评估结果）→ 预检清单 → 启动 → 实时日志
   ↓
压测（并发爬坡至 SLO 拐点）→ 得出最大可用并发 48 → 回写容量基准
   ↓
模型目录中以「自建」类型上架 → 灰度 10% → 观察 GPU 监控与错误率 → 全量
```

关键在于**每一步的输出是下一步的输入**，管理者不需要在五个页面之间手工抄参数。

**流程 B：处理一次 GPU 故障**

```
总览告警区 → GPU监控定位 node-04 GPU2（XID 48 / ECC 双位）
   ↓
查看受影响实例 → 一键驱逐并在健康节点重建 → 标记 GPU 为维护中
   ↓
容量评估器自动排除该卡 → 审计日志留痕
```

**流程 C：上游模型变更**

```
定时同步任务 → 检测到 3 项差异 → 总览告警"3 项上游变更待审"
   ↓
差异审阅弹窗（改价/新增/消失，含"仍有调用"提示）→ 勾选 → 灰度或直接应用
   ↓
审计日志记录操作人、变更前后值
```

---

## 8. 指标字典（口径必须先统一）

看板的可信度取决于口径，这部分建议在开发前与后端确认。

| 指标 | 定义 | 口径要点 |
|---|---|---|
| 请求数 | 到达网关并被计费的请求 | 是否含失败请求？建议分开统计「总请求/成功请求」 |
| Tokens | prompt + completion | **必须拆出 cache-read / cache-write**，否则成本对不上 |
| Tokens 使用率 | 已用 tokens ÷ 配额 tokens | 分母是套餐配额还是预算折算，需明确 |
| 成本 | 供应商实际计费 | 与「收入」分开；自建模型成本按 GPU 折旧+电费摊销 |
| 活跃用户 | 当日有 ≥1 次成功调用的唯一用户 | 按 UTC 日；机器人/测试账号需排除 |
| 工具调用次数 | 响应中 tool_calls 数组的元素总数 | 一次请求可能含多个调用，不等于请求数 |
| 工具调用成功率 | 客户端回传 tool_result 且无 error | 需要客户端配合上报，否则只能统计"生成了调用" |
| Agent 框架 | 由 User-Agent 或自定义头识别 | **需要网关侧新增埋点**，当前无此数据 |
| P95 延迟 | 端到端，含排队 | 区分 TTFT 与 E2E，两者不可混用 |
| GPU 水位 | 已分配显存 ÷ 总显存 | 分配 ≠ 实际使用，两个指标都要有 |
| 有效吞吐 | 满足 SLO 前提下的 tokens/s | 与「饱和吞吐」区分，后者无 SLO 约束 |

所有时间序列按**完整 UTC 周期**聚合，并暴露 `updated_at` 供 UI 标注数据新鲜度（沿用 `frontend/src/api/README.md` 已有约定）。

---

## 9. 落地映射

### 9.0 工程形态（已定）

仓库已重组为两个平行应用：

```
frontend/
├── web/      开发者门户 + 用户自助控制台（已存在，本方案不改）
└── admin/    管理平台（空目录，本方案落地于此）
```

这个结构本身就回答了原 §11 第 4 问：**admin 是独立构建产物，不是 web 的一条路由**。带来三个直接好处——管理后台代码不会发到公网用户浏览器；可在 nginx / 网关层对 admin 单独做 IP 白名单或 VPN 限制；两个应用可独立发版，改门户不必重新验证后台。

**共享代码怎么办 —— 结论：技术栈统一，但不共享代码。**

这两件事要分清楚：

- **统一的是依赖与版本**（§3.1 那张表）。两端 `package.json` 里 React/antd/Tailwind/Recharts/Router/Vite/TS 的版本号必须一致，避免出现"web 能跑 admin 跑不了"这种版本差异导致的疑难杂症。
- **不统一的是代码**。既然 UI 交互独立、样式写法也不同（web 手写语义类，admin 用 utility），抽 shared UI 包只会把两套不同约束绑死。admin 自建 `package.json` 与 `node_modules`，不引用 web 的任何模块。

唯一需要留意的是**非 UI 层的契约漂移**：两端都要调 `/api/v1`，都要解 `ApiEnvelope` 信封、`ApiError` 错误码。做法不是抽包，而是**以 `openapi.yaml` 为唯一真相源**，admin 自维护一份 TS 类型，两边都从同一份 YAML 校对。规模再大时再上 openapi 代码生成，现阶段手写足够。

```
frontend/
├── web/      已存在。本方案只升级其依赖版本，不改业务代码
└── admin/    新建。同一套依赖版本，独立 package.json 与 node_modules
```

代价是 antd/recharts 各装一份（约 1.2GB 磁盘，构建产物各自独立）。考虑到两个应用独立发版、独立限制访问，这笔账划算。

**版本一致性怎么保证**：两端 `package.json` 的版本号手工对齐即可，不引入 workspaces。建议在 P0 顺手加一个 CI 检查脚本，比对两个 `package.json` 的共同依赖版本是否一致，不一致就报错——靠自觉维护版本对齐必然会漂。

### 9.0.1 Tailwind 4 与 antd 6 的层级冲突（P0 必须先解决）

这是把 Tailwind 真正用起来后**一定会撞上**的问题，写在前面免得踩坑：

antd 5 以后用 CSS-in-JS 把样式注入 `<head>`，这些样式**不属于任何 CSS layer**。而 CSS 层叠规范里，无层样式的优先级**高于所有 layer**。Tailwind 4 的 utility 在 `utilities` layer 里。结果是：

```tsx
<Button className="mt-4 bg-brand" />   // ← bg-brand 不生效，被 antd 的样式压过
```

这就是"Tailwind 装了但用不起来"的根因，也可能正是 web 最终放弃 utility 改手写 CSS 的原因。

**解法**（antd 5.20+ 官方支持，v6 沿用）：用 `StyleProvider` 把 antd 样式也放进 layer，再显式声明层序，让 utilities 排在 antd 之后。

```tsx
// main.tsx
import { StyleProvider } from '@ant-design/cssinjs'

<StyleProvider layer>
  <ConfigProvider theme={adminTheme}>
    <App />
  </ConfigProvider>
</StyleProvider>
```

```css
/* index.css 第一行，顺序即优先级，靠后的赢 */
@layer theme, base, antd, components, utilities;
@import "tailwindcss";
```

**另一个坑是 preflight**：Tailwind 的 reset 会把 `button` 的 background 置为 transparent、border 置为 0。放进 layer 后 antd 能赢过 base 层，问题自然消解——但如果你选择不上 `StyleProvider layer`，就必须改为分段导入以跳过 preflight：

```css
@layer theme, base, components, utilities;
@import "tailwindcss/theme.css" layer(theme);
@import "tailwindcss/utilities.css" layer(utilities);
/* 不导入 preflight.css */
```

**推荐前者**（`StyleProvider layer` + 全量导入）：保留 preflight 的跨浏览器一致性，同时 utility 能正常覆盖 antd。P0 的第一个验收项就是这个——写一个 `<Button className="mt-4 bg-brand">` 确认样式真的生效，再往下做。

### 9.1 admin 应用的完整文件结构

```
frontend/admin/
├── package.json                  react · antd6 · tailwindcss4 · recharts · react-router7
│                                 + @ant-design/cssinjs（层级控制）
├── vite.config.ts                base:'/admin/'，proxy /api → 后端
├── postcss.config.mjs            @tailwindcss/postcss
├── tsconfig.json / .app.json / .node.json
├── index.html
├── .env.example                  VITE_API_BASE_URL / VITE_ENV_NAME / VITE_USE_MOCK
└── src/
    ├── main.tsx                  StyleProvider(layer) → ConfigProvider → AuthProvider
    ├── App.tsx                   仅路由表，不放页面逻辑
    ├── index.css                 @layer 声明 + @theme token（§3.2）+ 极少量语义类
    │
    ├── layout/
    │   ├── AdminShell.tsx        侧边栏 + 顶栏 + 环境色带
    │   ├── AdminSidebar.tsx      两级折叠导航
    │   ├── AdminTopbar.tsx       全局搜索 ⌘K、环境切换、告警铃、账号
    │   └── RequireAdmin.tsx      角色守卫（沿用 web 的 RequireAuth 模式）
    │
    ├── config/
    │   ├── navigation.ts         §2 的导航树（唯一真相源）
    │   ├── viz.ts                §4 色板的 JS 侧读取（从 CSS 变量读，不重复写死）
    │   ├── theme.ts              antd ConfigProvider token，值取自 @theme 变量
    │   └── metrics.ts            §8 指标字典的前端映射（单位、精度、口径提示）
    │
    ├── components/
    │   ├── charts/
    │   │   ├── MetricChart.tsx       ★ 统一图表封装
    │   │   ├── StatTile.tsx          统计磁贴
    │   │   ├── HeroFigure.tsx        主视觉大数字
    │   │   ├── Meter.tsx             计量条（比率对上限）
    │   │   ├── Heatmap.tsx           热力图 + 刻度图例
    │   │   └── DataTableToggle.tsx   「查看数据表」抽屉（对比度缓解）
    │   ├── common/
    │   │   ├── StatusBadge.tsx       状态色 + 图标 + 文字三重编码
    │   │   ├── DangerConfirm.tsx     输入资源名才可提交
    │   │   ├── FilterBar.tsx         时间/粒度/分组统一筛选行
    │   │   ├── EmptyState.tsx
    │   │   └── DiffReview.tsx        差异审阅（上游同步复用）
    │   └── infra/
    │       ├── GpuTopology.tsx       节点 × GPU 网格（填充=利用率，角标=状态）
    │       ├── CapacityCalculator.tsx 显存估算器 + 计算依据折叠面板
    │       ├── DownloadProgress.tsx  大文件下载（分片/续传/速率/剩余）
    │       ├── LogStream.tsx         SSE 日志流，可搜索可下载
    │       └── PrecisionCompare.tsx  FP16/FP8/INT4 方案对比表
    │
    ├── pages/
    │   ├── OverviewPage.tsx
    │   ├── analytics/{Usage,Users,Models,Tools}Page.tsx
    │   ├── catalog/{Models,Channels,Routing}Page.tsx
    │   ├── customers/{Users,Workspaces,Billing}Page.tsx
    │   ├── infra/{Cluster,Capacity,Registry,Deployments,Benchmark,Observability}Page.tsx
    │   └── system/{Settings,Rbac,Audit}Page.tsx
    │
    ├── api/
    │   ├── admin-contracts.ts    管理端类型（沿用 web 的 ApiEnvelope 信封）
    │   ├── admin-services.ts     按模块分组的端点函数
    │   └── sse.ts                下载进度 / 部署日志的 SSE 封装
    │
    └── mock/
        ├── index.ts              VITE_USE_MOCK 开关，默认 true
        ├── analytics.ts / catalog.ts / customers.ts / infra.ts
        └── generators.ts         时序、拓扑、压测曲线的确定性造数
```

**20 个页面、6 个图表组件、5 个基础设施组件。**

### 9.1.1 `MetricChart` —— 把图表规则编码进组件

这是整个 admin 应用最关键的一个文件。§4 的规则如果只写在文档里，第二个人接手时必然走样。做法是让**违规配置根本无法通过类型检查**：

```ts
type MetricChartProps = {
  form: 'line' | 'area' | 'bar' | 'stacked-bar' | 'heatmap'
  series: Series[]              // 长度 >8 时运行时抛错，提示折叠为「其他」
  unit: string                  // 所有 series 必须同 unit，否则抛错 → 从根上封死双轴
  colorJob: 'categorical' | 'sequential' | 'diverging' | 'emphasis'
  slo?: { value: number; label: string }   // 灰虚线阈值
  tableView?: boolean           // 用到低对比度槽位时强制为 true
}
```

内置行为，调用方无法关闭：
- 色板按 `config/viz.ts` 的固定槽位分配，**按 series.id 而非数组下标**取色（筛选不重新着色）。
- 折线/面积自动挂十字准星，柱/热力自动挂逐格提示。
- `series.length >= 2` 自动出图例；`<= 4` 同时做末端直接标注。
- 用到槽位 3/4/5（青/黄/品红）时自动把 `tableView` 置为 true 并渲染「查看数据表」入口。
- **没有 dual-axis 这个 prop**，物理上写不出双轴图。

### 9.1.2 Mock 策略

后端尚不存在，20 个页面不能等后端。约定：

- `VITE_USE_MOCK=true`（默认）时，`api/` 层返回 `mock/` 的数据，延迟 200–600ms 随机，**5% 概率返回错误**——强制每个页面从第一天就写加载态和错误态，而不是最后补。
- 造数用**固定种子**，保证同一页面每次刷新数据一致，方便截图评审和视觉回归。
- 绝不把 mock 和真实数据混在同一个列表里（沿用 `frontend/web/src/api/README.md` 已有约定）。
- 每个 mock 模块的导出类型必须来自 `admin-contracts.ts`，切真实后端时只改 `api/` 一层。

### 9.2 与 `opensource/new-api` 的能力映射

参考项目已有相当完整的管理后端，建议直接对接而非重写：

| 本方案模块 | new-api 已有 | 结论 |
|---|---|---|
| 模型目录 | `controller/model.go`、`model_sync.go`、`pricing.go`、`ratio_config.go` | ✅ 直接对接，UI 重做 |
| 供应商渠道 | `controller/channel.go`、`channel-test.go`、`channel-billing.go` | ✅ 直接对接 |
| 用量统计 | `controller/usedata.go`、`log.go` | ✅ 对接，但工具调用维度需新增埋点 |
| 用户管理 | `controller/user.go`、`group.go` | ✅ 直接对接 |
| 账务充值 | `controller/topup.go`、`billing.go`、`redemption.go` | ✅ 直接对接 |
| 性能指标 | `controller/perf_metrics.go`、`performance.go` | ⚠ 是网关性能，非 GPU 性能，需扩展 |
| 审计 | `controller/audit.go` | ✅ 直接对接 |
| **GPU 集群管理** | ❌ 无 | 🆕 全新，需自建 |
| **容量评估** | ❌ 无 | 🆕 全新 |
| **模型下载** | ❌ 无 | 🆕 全新 |
| **部署编排** | `deployment.go` 仅对接 io.net 第三方 GPU 云 | 🆕 自有集群需重做 |
| **压测** | ❌ 无 | 🆕 全新 |
| **GPU 可观测** | ❌ 无 | 🆕 需接 DCGM Exporter + Prometheus |

即：**管理平台的前四个板块主要是"给已有后端做一套更好的 UI"，私有化部署板块是"从零建设"**。工作量重心在后者。

### 9.3 后端新增接口（私有化部署部分）

```
GET    /admin/infra/nodes                  节点与 GPU 拓扑
GET    /admin/infra/gpus/metrics           GPU 时序指标（转发 Prometheus）
POST   /admin/infra/capacity/estimate      显存估算
GET    /admin/infra/registry/models        本地模型列表
POST   /admin/infra/registry/resolve       解析社区仓库元数据
POST   /admin/infra/registry/pull          发起下载（返回任务 ID）
GET    /admin/infra/registry/pull/{id}     下载进度（SSE 或轮询）
DELETE /admin/infra/registry/pull/{id}     取消下载
POST   /admin/infra/deployments            创建部署
GET    /admin/infra/deployments/{id}/logs  实时日志（SSE）
POST   /admin/infra/deployments/{id}/scale 扩缩容
POST   /admin/infra/benchmark/runs         发起压测
GET    /admin/infra/benchmark/runs/{id}    压测结果
```

下载进度与日志流建议用 **SSE** 而非轮询：长时任务、单向推送、浏览器原生支持，比 WebSocket 轻。

---

## 10. 执行方案

每个阶段都以**可在浏览器里点开看**为完成标准，不接受"组件写完了但没接进页面"。

### P-1 · 先把 web 升到目标版本 ✅ 已完成（2026-09-06）

**执行结果：4 步全部通过，只遇到 1 处破坏性变更。**

| 步骤 | 内容 | 结果 |
|---|---|---|
| 1 | 前 5 项（React/antd/icons/Tailwind/Recharts） | 无需操作，`^` 范围已装最新 |
| 2 | Vite `7.3.6 → 8.2.2` + plugin-react `5.2.0 → 6.1.1` | ⚠️ 1 处破坏性变更，已修复 |
| 3 | TypeScript `5.9.3 → 7.0.2` | 类型检查零错误，未改一行代码 |
| 4 | React Router `7.18.3 → 8.3.1` | 构建与类型检查均通过，未改一行代码 |

**唯一的破坏性变更**：Vite 8 把打包器从 rollup 换成了 **rolldown**，`build.rollupOptions.output.manualChunks` 不再接受对象形式，只接受函数。原配置直接报 `TypeError: manualChunks is not a function`。已改为函数形式（`frontend/web/vite.config.ts`），三个 chunk（react / antd / charts）的分割结果与升级前一致。

**构建耗时**：20.36s → 836ms。主要来自 TS 7 的原生编译器与 rolldown。

**已验证**：`tsc --noEmit` 零错误；`vite build` 通过，产物 chunk 分割正常；dev server 正常启动；`/`、`/models`、`/docs`、`/console`、`/login` 五条路径 HTTP 200；`main.tsx`、`App.tsx`、`AuthContext.tsx`、`AppHeader.tsx` 四个关键模块转译成功；`react-router` 依赖预打包解析正常；dev 日志无错误。

**未验证**：浏览器中的实际渲染与交互。构建通过不等于运行时行为不变，React Router 7 → 8 尤其需要人工确认——建议在浏览器里点一遍五条主路径，重点看路由跳转、登录后重定向、`RequireAuth` 守卫、`NavLink` 高亮这几处。

---

<details>
<summary>原计划（保留备查）</summary>

技术栈要统一到最新，就得先解决 §3.1 那 4 项大版本落后。**顺序上必须先升 web，再建 admin**：

web 是一个已知能跑通的完整应用（`npm run build` 通过、页面可用），拿它当升级的试验田，坏了能立刻断定是升级导致的。反过来先用新版本建 admin，一旦出问题，分不清是新版本的锅还是新代码的锅——在一个从零开始的项目里排查工具链问题，成本高得多。

| 步骤 | 内容 |
|---|---|
| 1 | `npm update` 拿到前 5 项（React/antd/icons/Tailwind/Recharts）的最新版，`^` 范围内无痛 |
| 2 | Vite `7 → 8` + `@vitejs/plugin-react 5 → 6`（构建工具链一起升，避免插件不兼容） |
| 3 | TypeScript `5.9 → 7.0`，跨两个大版本，预期是最容易出问题的一步 |
| 4 | React Router `7 → 8`，逐条核对路由 API 的破坏性变更 |

**每一步单独提交，单独验证**：`npm run build` 通过 + 手工点一遍首页/模型/文档/控制台/登录五条主路径。任何一步失败就停在那里，不要叠加多个升级再一起排查。

**验收**：web 构建通过、五条主路径可用、`package.json` 版本与 §3.1 表格一致。这时才有资格开 P0。

</details>

### P0 · 地基 ✅ 已完成（2026-09-06）

**5 条验收标准中 4 条已由自动化手段确认，第 1 条需人工过目。**

| # | 验收项 | 结果 |
|---|---|---|
| 1 | `<Button className="bg-status-good">` utility 生效 | ⏳ 静态证据齐全，需浏览器目视 |
| 2 | 改 `@theme` 色值，utility 与 antd 同时变色 | ✅ 三条路径均指向同一变量 |
| 3 | 20 条路由可达 + 两级折叠 + 环境色带 | ✅ 21/21 返回 200（含自检页） |
| 4 | `MetricChart` 通过 §附 12 条检查 | ✅ 规则已编码进组件 |
| 5 | 不同 `unit` 的 series 无法传入 | ✅ 类型上不存在该入口 |

**层序实测**（构建产物 CSS）：`properties → theme → base → antd → components → utilities`。antd 在 utilities 之前，utility 可覆盖。`main.tsx` 产物中确认存在 `StyleProvider` 与 `layer: true`。

**过程中发现并修复的两个真问题**：

1. **`@theme` 默认会 tree-shake 变量**。`--color-series-*` 与 `--color-seq-*` 因为只被 `viz.ts` 在运行时用 `getComputedStyle` 读取（Recharts 的 SVG `fill` 吃不了 utility 类），Tailwind 静态扫描不到，全部被摇掉。产物里读到空串会静默回落到 `viz.ts` 的兜底色，"改 CSS 变量全站生效"就名存实亡。改用 **`@theme static`** 后 26 个变量全部落地。
2. TypeScript 7 在 `strict` 下抓出两处空值处理疏漏（Recharts tooltip formatter 的 `value` 可能为 `undefined`、antd Select 的 `onSelect` 值可能为 `null`），已修正。

**自检页 `/admin/_selfcheck`** 不在导航中，作为常驻路由保留：改动 token 或样式层级后回来一眼即可确认地基是否还成立。

---

<details>
<summary>P0 原始交付清单（保留备查）</summary>

| 交付 | 文件 |
|---|---|
| admin 应用骨架跑起来 | `package.json`、`vite.config.ts`、`postcss.config.mjs`、`tsconfig*`、`index.html` |
| **Tailwind × antd 层级打通** | `main.tsx`（`StyleProvider layer`）+ `index.css`（`@layer` 声明） |
| 设计 token 单一来源 | `index.css` 的 `@theme` 块 + `config/theme.ts` + `config/viz.ts` |
| 布局与导航 | `layout/AdminShell` `AdminSidebar` `AdminTopbar` `RequireAdmin` |
| 导航与指标真相源 | `config/navigation.ts`、`config/metrics.ts` |
| 图表内核 | `components/charts/*` 全部 6 个 |
| 通用件 | `StatusBadge`、`DangerConfirm`、`FilterBar`、`EmptyState` |
| Mock 底座 | `mock/index.ts` + `generators.ts` |

**验收**（按顺序，前一条不过不往下做）：

1. `<Button className="mt-4 bg-brand">` 的 utility 类**确实生效**——层级问题解决，Tailwind 可用（§9.0.1）。
2. 改 `@theme` 里一个色值，utility 类和 antd 组件**同时**变色——token 单一来源成立。
3. 20 条路由全部可达并渲染占位页；侧边栏两级折叠正常；环境色带随 `VITE_ENV_NAME` 变色。
4. 用 `MetricChart` 画出的示例图通过 §附 的 12 条检查。
5. 给 `MetricChart` 传两个不同 `unit` 的 series 会**报错**——双轴从类型上被封死。

</details>

### P1 · 数据分析 ✅ 已完成（2026-09-06）

`OverviewPage`、`analytics/UsagePage`、`analytics/UsersPage`、`analytics/ModelsPage`

| 验收项 | 结果 |
|---|---|
| 总览告警区为空时整块消失 | ✅ `alerts.length > 0` 才渲染，无占位空框 |
| 切换度量复用同一张图且单位随之变化 | ✅ `MEASURE_UNIT` 映射驱动，radio 切换不并列 |
| 切换分组维度时已有系列不改色 | ✅ `ENTITY_SLOT` 注册表按实体 id 固定槽位 |
| 每张多系列图都能打开数据表 | ✅ 低对比度槽位自动强制 |

**新增支撑件**：`PageHeader`、`Panel`、`AsyncBoundary`、`useAsyncData`、`mock/analytics.ts`。

**实现中发现并加固的一处设计漏洞**：`MetricChart` 的缺省色槽是按数组下标分配的 —— 系列集合固定时没问题，但只要图支持筛选，筛掉一个系列就会让后面的全部左移换色，正好违反 §4「色彩跟随实体，绝不跟随排名」。类型上无法强制（单系列不需要 slot），因此改为**开发模式下显式 console.warn**，让问题在写代码时暴露，而不是等有人发现"筛完颜色全变了"。P1 的四个页面均已显式传 slot。

**几个刻意的图表决定**：转化漏斗用序数色阶而非 8 个分类色（阶段是有序的）；模型分析把「单位成本」与「P95 延迟」拆成两张图（量纲不同，双轴会编造相关性）；用户增长的新增/流失同为人数，共轴合法。

### P2 · 供给管理 ✅ 已完成（2026-09-06）

`catalog/ModelsPage`、`catalog/ChannelsPage`、`catalog/RoutingPage`，含 `DiffReview` 差异审阅弹窗。

| 验收项 | 结果 |
|---|---|
| 上游同步走三步，无一键写库路径 | ✅ 解析 → 审阅 → 选应用方式，`onApply` 只接收勾选项 |
| 「上游已消失」带近 7 日调用量提示 | ✅ 并**默认不勾选**，其余类型默认勾选 |
| 下架走 `DangerConfirm`（键入模型 ID） | ✅ 影响面里带调用量提示 |

**两个刻意的默认值**：差异审阅里「新增」「变更」默认勾选（低风险且通常就是要的），**「上游已消失」默认不勾** —— 误下架一个仍在被调用的模型，代价远高于漏下架一个。选中带调用量的下架项时，底部会额外弹出红色警示并把主按钮改为 danger。

**渠道页的连通性测试发真实请求**，返回原始响应体而非只给一个「成功/失败」，因为排查上游问题时需要看到 `429 rate limit` 这类具体信息。自动降权的**原因**也直接写在卡片上，否则管理者只看到「降级」不知道为什么。

### P3 · GPU 可观测 ✅ 已完成（2026-09-06）

`infra/ClusterPage`、`infra/ObservabilityPage`，含 `GpuTopology`。

| 验收项 | 结果 |
|---|---|
| 填充=利用率（顺序色）、角标=健康（状态色+图标），两通道不混用 | ✅ 且各自独立出图例 |
| 故障格叠加斜线纹理，灰度可辨 | ✅ `viz-hatch` |
| 利用率与显存共轴，温度与功耗独立成图 | ✅ |
| XID 错误码人类可读 | ✅ 释义 + **处置建议** |

**XID 表带处置建议而不只是释义**：XID 31（页错误）改软件就行，XID 48（ECC 双位）必须换卡，XID 79（掉总线）要重启节点 —— 三者代价差一个数量级。只翻译成英文标题等于没解决问题，管理者仍要去查 NVIDIA 文档才知道该做什么。

**角标只在非正常态出现**，不给每张健康卡都挂一个绿点 —— 满屏绿点会让真正的告警淹没在噪声里。

**实现中修的一处自造 bug**：温度图我一开始错用了 `percent` 单位（会渲染成「68.0%」），功耗也缺 W 后缀。已给 `MetricUnit` 补上 `celsius` 与 `watt`，并特别处理为不做 K/M 缩写 —— 温度功耗是小量级读数，缩写没有意义。

### P4 · 私有化链路前半段 ✅ 已完成（2026-09-06）

`infra/CapacityPage`（含 `CapacityCalculator`、`PrecisionCompare`）、`infra/RegistryPage`（含 `DownloadProgress`），计算核心抽在 `lib/capacity.ts`。

| 验收项 | 结果 |
|---|---|
| 「计算依据」展示完整公式与代入值 | ✅ 折叠面板逐行给出算式与本次数值 |
| 结果卡片标注估算不确定性 | ✅ 标注 ±10–15% 并要求压测校准 |
| 解析后自动回填结构参数到评估器 | ✅ 经 URL query 传递，页面顶部提示已回填 |
| 门控仓库在解析阶段提示令牌 | ✅ 未填令牌时「开始拉取」禁用 |
| 双向计算 | ⚠️ 见下方说明，改为可行域热力图 |

**实现中发现的一个严重建模问题（已修正）**：按"所有并发请求都占满 max_model_len"计算 KV Cache 是理论最坏情况，但 vLLM 的 PagedAttention 是按实际 token 分页分配的。以 Qwen3-72B / 32K / 64 并发为例，最坏情况需 **640 GB** KV，而 30% 平均占用率下只要 **192 GB** —— 相差 3 倍以上，直接决定买 8 张卡还是 24 张卡。若不修正，这个评估器会让人多买好几倍的卡。已增加**平均上下文占用率**参数（默认 30%），并在提示里说明应按部署后实测的平均序列长度回调。

**关于「双向计算」**：原计划的"给定卡数反推最大模型"存在循环依赖 —— KV Cache 取决于层数与 KV 头数，而这些又取决于模型大小，无法闭式求解。改为**并发 × 上下文可行域热力图**：固定模型与卡型，直接显示哪些 (并发, 上下文) 组合放得下、单卡占用多少、超容量的格子叠加纹理。这比一个需要循环迭代才能算出的"最大参数量"更贴近实际决策。

**KV 头数在表单里单独带了说明**：GQA/MQA 模型的 KV 头数远小于注意力头数（Qwen3-72B 是 8 vs 64），用错会把 KV Cache 估高 8 倍。这是整个公式里最容易出错的一项。

### P5 · 私有化链路后半段 ✅ 已完成（2026-09-06）

`infra/DeploymentsPage`（含 `LogStream`）、`infra/BenchmarkPage`。

| 验收项 | 结果 |
|---|---|
| 最后一步是预检清单而非直接启动 | ✅ 5 步向导，第 5 步为 6 项预检 |
| 日志流式 + 加载阶段用不确定进度 | ✅ 跳动光标，无假进度条 |
| 主结论是「最大可用并发」一个大数字 | ✅ `HeroFigure`，曲线降为佐证 |
| 吞吐与延迟分成两张图 | ✅ tok/s 与 ms 量纲不同，不共轴 |
| 压测结果可回写容量评估基准 | ✅ |

**SLO 拐点是本页的核心概念**：吞吐随并发一直涨到饱和，但延迟在某点后急剧恶化。容量规划要的不是"最大吞吐"，而是"仍满足 SLO 的最大并发"——前者是拿用户体验换来的数字。页面同时给出三个量：有效吞吐（拐点处）、饱和吞吐（违约处）、以及"继续加压能多榨出的吞吐百分比"，让这笔交易明码标价。SLO 阈值可现场调整，拐点随之重算。

**TTFT 三条分位线共轴是合法的**（同为 ms），与吞吐-延迟不共轴不矛盾——判据始终是量纲，不是"看起来像不像一类东西"。

**日志加载阶段刻意不给百分比**：safetensors 分片加载没有可靠进度可报，匀速前进的假进度条会在真卡住时误导人以为一切正常。用跳动光标表达"还在动"是诚实的做法。

**部署失败原因具体到可操作**：不是"启动失败"，而是"CUDA out of memory：请求 24.0 GB，设备可用 18.2 GB。该卡已因 ECC 双位错误被隔离，请改选其他 GPU"——直接指向下一步动作。

**§7 流程 A 的端到端闭环现已全线打通**：模型仓库解析 → 容量评估（回填结构参数）→ 部署向导（带入 TP 与精度）→ 预检 → 流式日志 → 压测 → 回写容量基准。管理者全程无需手抄任何参数。

### P6 · 收尾 ✅ 已完成（2026-09-06）

`analytics/ToolsPage`、`customers/*` 3 页、`system/*` 3 页。**至此 20 个页面全部实现，占位组件已删除。**

**Agent 框架维度做成了诚实的空状态**，没有编数据。网关当前不记录调用方框架（§8 已标注该维度需新增埋点），与其画一张看起来合理、实则凭空捏造的分布图，不如把缺口明确摆出来 —— 假数据会被拿去做决策，空状态不会。区块内说明了需要什么埋点、由谁配合上报。工具调用页其余三个维度（调用量 Top、成功率、链长分布）数据来源成立，正常展示。

**几处与用户端刻意不同的处理**：

| 位置 | 处理 | 原因 |
|---|---|---|
| 平台设置 | 显式保存 + 底部常驻提示条 | 每一项都影响全平台，拨错一个开关就改变对全体用户的数据承诺 |
| 角色权限 | 完整能力矩阵表 | 把「运维到底能干什么」写清楚，不留给人猜 |
| 审计日志 | 破坏性操作单独标记 + 一键筛选 | 事故复盘第一个问题永远是"谁动了什么"，不该埋在几百条查询记录里 |
| 用户停用 / 退款 | `DangerConfirm` + 影响面提示 | 停用会中断生产请求，退款不可撤销 |

审计日志的示例数据刻意串联了前面各阶段的操作（上游变更、自动驱逐、渠道降权、模型下架、压测、拉取权重），验证留痕链路是连贯的。

### 里程碑

P0–P6 全部完成（2026-09-06）。最终交付：**65 个源文件 · 20 个页面 · 19 个组件 · 约 9,000 行**，构建与类型检查通过，21 条路由（含自检页）全部可达。

后端接入前的现状：全部页面运行在 `mock/` 数据上（`VITE_USE_MOCK=true`），mock 层注入 5% 失败率以保证每页的加载态与错误态都被真实走到过。

### 收尾：补齐三项欠账（2026-09-06）

P0–P6 交付后，方案里承诺过但实际没做的三件事已补上：

**1. `api/` 层建成，页面与 mock 解耦。** 此前 20 个页面直接 import `mock/`，与 §9.1.2 的架构约定不符。现在：

- `api/contracts.ts` 成为全部管理端类型的单一来源，mock 与页面都从这里取类型
- `api/services.ts` 是「mock 或真实请求」的唯一分流点，端点路径与 §9.3 对齐
- `api/client.ts` 复用与 web 相同的信封与错误码约定；`api/sse.ts` 备好下载进度与部署日志的流式订阅
- **页面对 mock 的直接数据调用从 18 处降到 0**，23 处调用全部走 `api`。切后端只需 `VITE_USE_MOCK=false`

顺带把混在 mock 里的**领域知识**移了出去：XID 释义表 → `lib/gpu.ts`，SLO 判定 → `lib/benchmark.ts`，角色权限矩阵与各类 label → `api/contracts.ts`。这些是产品定义，不是造出来的假数据，放在 mock 里会随 mock 一起被删掉。`mock/` 现在只剩造数。

**2. 版本一致性检查脚本** `frontend/scripts/check-versions.mjs`。比对 web 与 admin 的共同依赖（当前 14 个），不一致即非零退出。§9.0 说过"靠自觉维护版本对齐必然会漂"，现在有脚本兜住。

**3. `openapi.yaml` 提升为仓库级契约**，从 `web/src/api/` 移到 `frontend/contracts/`。它原先的位置暗示"这是 web 的东西"，实际上是两端共同的约定。

**一个诚实标注的设计债**：契约里目前仍混有 `statusKind` 这类前端 UI 状态字段。真实后端不该关心前端用什么颜色，应只返回业务状态（`running`/`failed`）由前端映射。`contracts.ts` 底部已提供 `deploymentUiStatus` 等映射函数，接后端时应删掉契约里的 UI 字段改为调用映射。现在保留是为了不阻塞 mock 阶段 —— 这是要还的。

---

## 10.1 与 `frontend/web` 的关系

| 事项 | 约定 |
|---|---|
| 代码复用 | **无**。禁止 admin `import '../web/src/...'`，两个 app 各自独立 `node_modules` |
| UI / 交互 | 完全独立。admin 是 Tailwind utility-first，web 是手写语义类，互不迁移 |
| 设计 token | 各自维护。色值一致靠本文档，不靠共享文件 |
| API 契约 | 以 `frontend/web/src/api/openapi.yaml` 为唯一真相源，admin 自维护 TS 类型并对照校验 |
| 认证 | 共用同一套会话 Cookie（同源前提下），admin 额外校验角色；admin 自带登录页，不跳 web |
| 部署 | 独立产物；admin 建议走内网域名或网关白名单，不与门户同域暴露 |
| 版本 | 独立发版，互不阻塞 |

**唯一的跨端风险是 API 契约漂移**。缓解办法是把 `openapi.yaml` 提升为仓库级契约（建议后续移到 `frontend/contracts/` 或仓库根），而不是留在 web 的 src 里——现在它的位置暗示"这是 web 的东西"，但实际上是两端共同的约定。这是一个值得在 P0 顺手做掉的小重构。

---

## 11. 需要你决策的问题

以下几点会实质影响设计，我按当前理解做了默认假设，请确认或推翻：

**B. 推理框架**：默认假设 vLLM 为主、SGLang 为辅。若要支持 TensorRT-LLM 或 LMDeploy，容量评估公式和部署参数表单都需要分支——不同框架的显存开销模型差异不小。

**C. 集群编排底座**：默认假设 Kubernetes + GPU Operator。若是裸机 + systemd 或 Docker Compose，部署实例页的扩缩容、副本、驱逐等概念都要重新定义。

**D. 监控数据源**：默认假设 DCGM Exporter + Prometheus。若已有其他体系（Zabbix、云厂商监控），指标名和查询方式需调整。

**E. 多租户边界**：单一平台方视角，还是需要"代理商/子管理员只能看自己名下用户"？后者会让每个数据接口都加租户过滤，成本显著上升。

**F. 工具调用数据来源**：当前网关无法区分 Agent 框架。接受在网关侧新增 User-Agent 埋点（需 SDK/客户端配合），还是该维度先降级为"能识别就识别"？

> 已定项：
> - ~~web 的升级时机~~ → 选方案一，**P-1 已执行完毕**，web 全栈升至最新版（§10.P-1）。
> - ~~管理平台与门户的部署关系~~ → 仓库已重组为 `frontend/web` + `frontend/admin`，独立应用独立构建。
> - ~~共享代码方案~~ → 技术栈统一，代码不共享；UI 交互完全独立（§9.0）。
> - ~~admin 技术栈~~ → 与 web 统一，取最新版：React 19.2 + antd 6.6 + Tailwind 4.3 + Router 8.3 + Vite 8.2 + TS 7.0（§3.1）。
> - ~~图表库~~ → **Recharts**（3.10.1），与 web 统一。GPU 热力图与拓扑用 Recharts 画会比较费劲，但统一栈的收益大于换库；实现上用自定义 SVG 层解决，不额外引入 ECharts。

---

## 附：本方案的图表规则速查

给实现者的检查清单，每张图交付前对照：

- [ ] 没有双 Y 轴。不同量纲拆图或归一化。
- [ ] 分类色按固定槽位分配，筛选不重新着色。
- [ ] 系列数 ≤8；超出折叠为「其他」或分面。散点/气泡类 ≤3。
- [ ] 顺序色是单一色相，不是彩虹。
- [ ] 发散色中点是中性灰，两极一暖一冷。
- [ ] 状态色只用于状态，且必带图标+文字。
- [ ] 用到青/黄/品红槽位的图，提供直接标注或数据表。
- [ ] ≥2 系列有图例；≤4 系列同时直接标注。
- [ ] 折线/面积有十字准星，柱/点/热力有逐格提示。
- [ ] 单个数字用统计磁贴，不用单柱图；单个比率用计量条，不用两片饼图。
- [ ] 网格类别 >7 时改用表格或表格+图。
- [ ] 渲染后实际看过一眼，确认没有标签重叠和溢出。
